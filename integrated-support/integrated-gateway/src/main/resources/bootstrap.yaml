spring:
  application:
    # 服务逻辑名称
    name: gateway
  profiles:
    active: dev
  cloud:
    nacos:
      # 注册中心
      server-addr: 127.0.0.1:8848
      discovery:
        group: web
      config:
        group: web
        name: ${spring.application.name}
        config-long-poll-timeout: 10000
        file-extension: yaml
        max-retry: 1